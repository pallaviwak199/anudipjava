package Document;
public interface Shape {//interface shape

	//abstract method
	double RectangleArea(double length,double bredth);
	double SquareArea(double length);
	double CircleArea(double radius);
}